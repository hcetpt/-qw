程序类型
=============

.. toctree::
   :maxdepth: 1
   :glob:

   程序_*

要查看所有程序类型的列表，请参阅
:ref:`libbpf` 文档中的 :ref:`program_types_and_elf`。 

这里的 `:ref:` 是 reStructuredText 的引用语法，用于链接到文档的其他部分。`prog_*` 和 `program_types_and_elf` 应该是文档中定义的标签或章节名称，它们会被转换为相应的中文标题或章节名称，如果这些标签或章节名称在中文版的文档中有对应的翻译的话。但是在这个例子中，具体的翻译取决于你的文档上下文和你如何定义了这些标签或章节名称。
