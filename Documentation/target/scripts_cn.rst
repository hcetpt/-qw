目标导出设备脚本
---------------------------

.. literalinclude:: target-export-device
    :language: shell

这里的 ``literalinclude`` 是一种标记语言（例如 Sphinx 文档生成工具使用的 reStructuredText）指令，用于直接包含文件内容。这行代码的意思是将 `target-export-device` 文件的内容以 Shell 脚本的形式插入到当前文档中。

如果你需要进一步解释或具体翻译，请提供更多的上下文或者具体的脚本内容。
