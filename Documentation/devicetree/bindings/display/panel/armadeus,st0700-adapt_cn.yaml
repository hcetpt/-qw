```yaml
# SPDX-许可证标识符: GPL-2.0
%YAML 1.2
---
$id: http://devicetree.org/schemas/display/panel/armadeus,st0700-adapt.yaml#
$schema: http://devicetree.org/meta-schemas/core.yaml#

title: Armadeus ST0700 适配器

description:
  这是一个配备有 Santek ST0700I5Y-RBSLW 7.0英寸 WVGA (800x480) TFT 的适配板
maintainers:
  - '"Sébastien Szymanski" <sebastien.szymanski@armadeus.com>'
  - Thierry Reding <thierry.reding@gmail.com>

allOf:
  - $ref: panel-common.yaml#

properties:
  compatible:
    const: armadeus,st0700-adapt

  power-supply: true
  backlight: true
  port: true

additionalProperties: false

required:
  - compatible
  - power-supply
```
请注意，上述翻译保留了原始 YAML 文件的结构和格式。其中一些术语如 `$ref` 和 `const` 等未进行翻译，因为它们是 YAML 文件中特定的关键词，通常在所有语言环境中保持一致。
