下面是给定内容的中文翻译：

```yaml
# SPDX-文件许可标识符: (GPL-2.0-only 或 BSD-2-Clause)
%YAML 1.2
---
$id: http://devicetree.org/schemas/display/bridge/nxp,tda998x.yaml#
$schema: http://devicetree.org/meta-schemas/core.yaml#

title: NXP TDA998x HDMI 发射器

maintainers:
  - Russell King <linux@armlinux.org.uk>

properties:
  compatible:
    const: nxp,tda998x

  reg:
    maxItems: 1

  interrupts:
    maxItems: 1

  video-ports:
    $ref: /schemas/types.yaml#/definitions/uint32
    default: 0x230145
    maximum: 0xffffff
    description:
      24位值，用于定义视频控制器输出如何连接到 TDA998x 输入端
audio-ports:
    description:
      每个 DAI 的两个值数组（Documentation/sound/soc/dai.rst）
实现允许一个或两个 DAI
如果定义了两个 DAI，则它们必须是不同类型的
$ref: /schemas/types.yaml#/definitions/uint32-matrix
    minItems: 1
    maxItems: 2
    items:
      items:
        - description: |
            第一个值定义 DAI 类型：TDA998x_SPDIF 或 TDA998x_I2S
            （参见 include/dt-bindings/display/tda998x.h）
enum: [ 1, 2 ]
        - description:
            第二个值定义当使用特定 DAI 时 tda998x AP_ENA 寄存器的内容
maximum: 0xff

  '#sound-dai-cells':
    enum: [ 0, 1 ]

  nxp,calib-gpios:
    maxItems: 1
    description:
      校准 GPIO，必须与用于 TDA998x 中断引脚的 GPIO 对应
port:
    $ref: /schemas/graph.yaml#/properties/port
    description: 并行输入端口

  ports:
    $ref: /schemas/graph.yaml#/properties/ports

    properties:
      port@0:
        type: object
        description: 并行输入端口

      port@1:
        type: object
        description: HDMI 输出端口

required:
  - compatible
  - reg

oneOf:
  - required:
      - port
  - required:
      - ports

additionalProperties: false

examples:
  - |
    #include <dt-bindings/display/tda998x.h>
    #include <dt-bindings/interrupt-controller/irq.h>

    i2c {
        #address-cells = <1>;
        #size-cells = <0>;

        tda998x: hdmi-encoder@70 {
            compatible = "nxp,tda998x";
            reg = <0x70>;
            interrupt-parent = <&gpio0>;
            interrupts = <27 IRQ_TYPE_EDGE_FALLING>;
            video-ports = <0x230145>;

            #sound-dai-cells = <1>;
                         /* DAI 格式 / AP_ENA 寄存器值 */
            audio-ports = <TDA998x_SPDIF 0x04>,
                          <TDA998x_I2S 0x03>;

            port {
                tda998x_in: endpoint {
                    remote-endpoint = <&lcdc_0>;
                };
            };
        };
    };
```
```
