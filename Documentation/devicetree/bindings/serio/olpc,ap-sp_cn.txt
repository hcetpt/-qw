这段代码描述了OLPC AP-SP串行接口的配置属性。下面是中文翻译：

所需属性：
- 兼容性: "olpc,ap-sp"
- 注册器 (reg)：SoC 的 WTM 寄存器的基本地址和长度
- 中断 (interrupts)：SP-AP 中断

示例：
```
ap-sp@d4290000 { 
	兼容性 = "olpc,ap-sp"; 
	注册器 = <0xd4290000 0x1000>; 
	中断 = <40>; 
}
```

这里 `@d4290000` 表示设备的基地址。
