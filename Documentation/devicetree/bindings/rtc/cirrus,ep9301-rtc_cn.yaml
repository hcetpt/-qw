```yaml
# SPDX-许可证标识符: (GPL-2.0-only 或 BSD-2-Clause)
%YAML 1.2
---
$id: http://devicetree.org/schemas/rtc/cirrus,ep9301-rtc.yaml#
$schema: http://devicetree.org/meta-schemas/core.yaml#

title: Cirrus EP93xx 实时时钟控制器

maintainers:
  - Hartley Sweeten <hsweeten@visionengravers.com>
  - Alexander Sverdlin <alexander.sverdlin@gmail.com>

allOf:
  - $ref: rtc.yaml#

properties:
  compatible:
    oneOf:
      - const: cirrus,ep9301-rtc
      - items:
          - enum:
              - cirrus,ep9302-rtc
              - cirrus,ep9307-rtc
              - cirrus,ep9312-rtc
              - cirrus,ep9315-rtc
          - const: cirrus,ep9301-rtc

  reg:
    maxItems: 1

required:
  - compatible
  - reg

unevaluatedProperties: false

examples:
  - |
    rtc@80920000 {
        compatible = "cirrus,ep9301-rtc";
        reg = <0x80920000 0x100>;
    };
```

请注意，某些关键字如 `const`, `oneOf`, `items`, `enum`, `maxItems`, `required`, 和 `unevaluatedProperties` 保持英文状态，因为这些是 YAML 和 JSON Schema 的规范术语。
