```yaml
# SPDX-文件许可人: (GPL-2.0-only 或 BSD-2-Clause)
%YAML 1.2
---
$id: http://devicetree.org/schemas/fpga/altr,socfpga-fpga2sdram-bridge.yaml#
$schema: http://devicetree.org/meta-schemas/core.yaml#

title: Altera FPGA 至 SDRAM 桥接器

maintainers:
  - Xu Yilun <yilun.xu@intel.com>

allOf:
  - $ref: fpga-bridge.yaml#

properties:
  compatible:
    const: altr,socfpga-fpga2sdram-bridge

  reg:
    maxItems: 1

required:
  - compatible

unevaluatedProperties: false

examples:
  - |
    fpga-bridge@ffc25080 {
        compatible = "altr,socfpga-fpga2sdram-bridge";
        reg = <0xffc25080 0x4>;
        bridge-enable = <0>;
    };
```

请注意，`SPDX-文件许可人:` 标签的翻译可能需要根据具体上下文调整以符合中文习惯，这里的翻译为“SPDX-文件许可人:”是一种直接的中文翻译。在实际使用中，你可能会看到类似“SPDX-许可证标识符:”这样的表达。此外，“Altera FPGA 至 SDRAM 桥接器”是对标题的一种较为常见的中文翻译方式。
