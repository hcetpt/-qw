# SPDX-许可证标识符: (GPL-2.0 或 BSD-2-Clause)
%YAML 1.2
---
$id: http://devicetree.org/schemas/fpga/fpga-bridge.yaml#
$schema: http://devicetree.org/meta-schemas/core.yaml#

标题: FPGA 桥接器

维护者:
  - Michal Simek <michal.simek@amd.com>

属性:
  $nodename:
    模式: "^fpga-bridge(@.*|-([0-9]|[1-9][0-9]+))?$"

  bridge-enable:
    描述: |
      如果驱动程序应在启动时禁用桥接器，则为 0
      如果驱动程序应在启动时启用桥接器，则为 1
      默认情况下是保持桥接器在当前状态
$ref: /schemas/types.yaml#/definitions/uint32
    枚举: [ 0, 1 ]

其他属性: true

示例:
  - |
    fpga-bridge {
        bridge-enable = <0>;
    };
