```yaml
# SPDX-许可证标识符: (GPL-2.0-only 或 BSD-2-Clause)
%YAML 1.2
---
$id: http://devicetree.org/schemas/leds/onnn,ncp5623.yaml#
$schema: http://devicetree.org/meta-schemas/core.yaml#

title: ON 半导体 NCP5623 多 LED 驱动器

maintainers:
  - Abdel Alkuor <alkuor@gmail.com>

description:
  NCP5623 三输出 I2C 控制 LED 驱动器
https://www.onsemi.com/pdf/datasheet/ncp5623-d.pdf

properties:
  compatible:
    enum:
      - onnn,ncp5623

  reg:
    const: 0x38

  multi-led:
    type: object
    $ref: leds-class-multicolor.yaml#
    unevaluatedProperties: false

    properties:
      "#address-cells":
        const: 1

      "#size-cells":
        const: 0

    patternProperties:
      "^led@[0-2]$":
        type: object
        $ref: common.yaml#
        unevaluatedProperties: false

        properties:
          reg:
            minimum: 0
            maximum: 2

        required:
          - reg
          - color

    required:
      - "#address-cells"
      - "#size-cells"

required:
  - compatible
  - reg
  - multi-led

additionalProperties: false

examples:
  - |
    #include <dt-bindings/leds/common.h>

    i2c {
        #address-cells = <1>;
        #size-cells = <0>;

        led-controller@38 {
            compatible = "onnn,ncp5623";
            reg = <0x38>;

            multi-led {
                color = <LED_COLOR_ID_RGB>;

                #address-cells = <1>;
                #size-cells = <0>;

                led@0 {
                    reg = <0>;
                    color = <LED_COLOR_ID_RED>;
                };

                led@1 {
                    reg = <1>;
                    color = <LED_COLOR_ID_GREEN>;
                };

                led@2 {
                    reg = <2>;
                    color = <LED_COLOR_ID_BLUE>;
                };
            };
        };
    };
```

以上是将原文翻译成中文的 YAML 格式文件。请注意，某些英文缩写和专有名词保持了原文的形式，以避免歧义或技术上的错误。
