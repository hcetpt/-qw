```yaml
# SPDX-许可证标识符: (GPL-2.0-only 或 BSD-2-Clause)
%YAML 1.2
---
$id: http://devicetree.org/schemas/soc/qcom/qcom,pbs.yaml#
$schema: http://devicetree.org/meta-schemas/core.yaml#

title: 高通技术公司可编程启动排序器

maintainers:
  - Anjelique Melendez <quic_amelende@quicinc.com>

description: |
  高通技术公司的可编程启动排序器（PBS）
  支持根据请求触发客户的上电和下电序列
properties:
  compatible:
    items:
      - enum:
          - qcom,pmi632-pbs
      - const: qcom,pbs

  reg:
    maxItems: 1

required:
  - compatible
  - reg

additionalProperties: false

examples:
  - |
    #include <dt-bindings/spmi/spmi.h>

    pmic@0 {
      reg = <0x0 SPMI_USID>;
      #address-cells = <1>;
      #size-cells = <0>;

      pbs@7400 {
        compatible = "qcom,pmi632-pbs", "qcom,pbs";
        reg = <0x7400>;
      };
    };
```

请注意，某些部分如注释、代码示例等可能没有直接的中文翻译版本，因此这部分内容保持了原文的形式。上述翻译尽可能保留了原意和技术细节。
