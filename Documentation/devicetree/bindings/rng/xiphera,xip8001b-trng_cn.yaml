```yaml
# SPDX-许可证标识符: (GPL-2.0-only 或 BSD-2-Clause)
%YAML 1.2
---
$id: http://devicetree.org/schemas/rng/xiphera,xip8001b-trng.yaml#
$schema: http://devicetree.org/meta-schemas/core.yaml#

title: Xiphera XIP8001B-trng

maintainers:
  - Atte Tommiska <atte.tommiska@xiphera.com>

description: |
  Xiphera 基于 FPGA 的真随机数生成器知识产权核心

properties:
  compatible:
    const: xiphera,xip8001b-trng

  reg:
    maxItems: 1

required:
  - compatible
  - reg

additionalProperties: false

examples:
  - |
    rng@43c00000 {
        compatible = "xiphera,xip8001b-trng";
        reg = <0x43c00000 0x10000>;
    };
```

请注意，这里使用了 YAML 格式来定义设备树规范。上述代码描述了一个基于 Xiphera XIP8001B-trng 的真随机数生成器的设备树条目，并规定了其兼容性和注册地址范围。
