```yaml
# SPDX-License-Identifier: (GPL-2.0 OR BSD-2-Clause)
%YAML 1.2
---
$id: 'http://devicetree.org/schemas/bus/qcom,ssbi.yaml'
$schema: 'http://devicetree.org/meta-schemas/core.yaml'

title: 高通单线串行总线接口 (SSBI)

description:
  某些高通MSM设备包含一个点对点的串行总线，用于与有限范围内的设备（主要是电源管理芯片）通信。
maintainers:
  - Andy Gross <agross@kernel.org>
  - Bjorn Andersson <andersson@kernel.org>

properties:
  compatible:
    const: qcom,ssbi

  reg:
    maxItems: 1

  qcom,controller-type:
    description:
      指示控制器应使用哪种SSBI总线变体来与从设备进行通信。所选类型由连接的从设备确定。
    enum:
      - ssbi
      - ssbi2
      - pmic-arbiter

  pmic:
    $ref: '/schemas/mfd/qcom-pm8xxx.yaml'

required:
  - compatible
  - reg
  - qcom,controller-type

additionalProperties: false

examples:
  - |
    #include <dt-bindings/interrupt-controller/irq.h>
    ssbi@c00000 {
        compatible = "qcom,ssbi";
        reg = <0x00c00000 0x1000>;
        qcom,controller-type = "pmic-arbiter";

        pmic {
            compatible = "qcom,pm8821";
            interrupt-parent = <&msmgpio>;
            interrupts = <76 IRQ_TYPE_LEVEL_LOW>;
            #interrupt-cells = <2>;
            interrupt-controller;
            #address-cells = <1>;
            #size-cells = <0>;
        };
    };
```
This is a translation of the provided YAML file into Chinese, maintaining the structure and meaning as closely as possible.
