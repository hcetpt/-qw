什么：`tcp_dma_copybreak` 系统控制参数  
日期：在内核版本 v3.13 中移除  
联系人：Dan Williams <dan.j.williams@intel.com>  
描述：
    这个参数以前表示以字节为单位的套接字读取大小的下限，这些读取操作将会卸载到 DMA (直接内存访问) 复制引擎上。由于存在一致性问题，即 CPU 可能在 DMA 操作进行中访问缓冲区，因此该参数已被移除。
