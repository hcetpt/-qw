### /sys/devices/system/cpu/dscr_default
- **日期**: 2014年5月13日
- **内核版本**: v3.15.0
- **描述**: 写入操作等同于在所有CPU上写入 `/sys/devices/system/cpu/cpuN/dscr`
- **读取**: 返回最后一次写入的值或0
- **说明**: 此值并非全局默认值，而是一种同时设置所有CPU默认值的方法
- **值**: 64位无符号整数（位字段）

### /sys/devices/system/cpu/cpu[0-9]+/dscr
- **日期**: 2014年5月13日
- **内核版本**: v3.15.0
- **描述**: CPU上的数据流控制寄存器(DSCR)的默认值
- **说明**: 此默认值用于内核执行时以及任何未自行设置DSCR的进程
- **如果进程设置了DSCR**: 通过直接访问SPR设置的值将被持久化，并在该进程执行的任何CPU上使用（覆盖此处描述的值）
- **如果由进程设置**: 将被子进程继承
- **值**: 64位无符号整数（位字段）

### /sys/devices/system/cpu/cpuX/topology/physical_package_id
- **描述**: cpuX的物理包ID。通常对应于物理插槽编号，但实际值取决于架构和平台
- **值**: 整数

### /sys/devices/system/cpu/cpuX/topology/die_id
- **描述**: cpuX的CPU晶片ID。通常是硬件平台的标识符（而非内核的）。实际值取决于架构和平台
- **值**: 整数

### /sys/devices/system/cpu/cpuX/topology/core_id
- **描述**: cpuX的CPU核心ID。通常是硬件平台的标识符（而非内核的）。实际值取决于架构和平台
- **值**: 整数
### 值类型：整数

#### 路径：/sys/devices/system/cpu/cpuX/topology/cluster_id
##### 描述：cpuX 所属的集群ID。通常这是硬件平台的标识符（而非内核的）。实际值依赖于具体的架构和平台。
##### 值类型：整数

#### 路径：/sys/devices/system/cpu/cpuX/topology/book_id
##### 描述：cpuX 的书籍ID。通常这是硬件平台的标识符（而非内核的）。实际值依赖于具体的架构和平台。仅在s390平台上使用。
##### 值类型：整数

#### 路径：/sys/devices/system/cpu/cpuX/topology/drawer_id
##### 描述：cpuX 的抽屉ID。通常这是硬件平台的标识符（而非内核的）。实际值依赖于具体的架构和平台。仅在s390平台上使用。
##### 值类型：整数

#### 路径：/sys/devices/system/cpu/cpuX/topology/core_cpus
##### 描述：同一核心内部CPU的内核映射（旧名称："thread_siblings"）。
##### 值类型：十六进制位掩码

#### 路径：/sys/devices/system/cpu/cpuX/topology/core_cpus_list
##### 描述：同一核心内部CPU的人类可读列表（旧名称："thread_siblings_list"）。格式例如：0-3, 8-11, 14,17。
##### 值类型：十进制列表

#### 路径：/sys/devices/system/cpu/cpuX/topology/package_cpus
##### 描述：共享相同物理包ID的CPU的内核映射。
**已弃用名称:** "core_siblings"

- **值:** 十六进制位掩码
- **位置:** `/sys/devices/system/cpu/cpuX/topology/package_cpus_list`
- **描述:** 具有相同物理包ID的CPU的人可读列表。格式如0-3, 8-11, 14,17

**已弃用名称:** "core_siblings_list"

- **值:** 十进制列表
- **位置:** `/sys/devices/system/cpu/cpuX/topology/die_cpus`
- **描述:** 同一芯片内CPU的内核映射。

- **值:** 十六进制
- **位置:** `/sys/devices/system/cpu/cpuX/topology/ppin`
- **描述:** 每个插槽受保护处理器的库存编号。

- **值:** 十六进制位掩码
- **位置:** `/sys/devices/system/cpu/cpuX/topology/die_cpus_list`
- **描述:** 同一芯片内的CPU的人可读列表。格式如0-3, 8-11, 14,17
### 值：十进制列表
#### 何项：`/sys/devices/system/cpu/cpuX/topology/cluster_cpus`
#### 描述：同一集群内CPU的内核内部映射

### 值：十六进制位掩码
#### 何项：`/sys/devices/system/cpu/cpuX/topology/cluster_cpus_list`
#### 描述：同一集群内CPU的人类可读列表
#### 格式如：0-3, 8-11, 14,17

### 值：十进制列表
#### 何项：`/sys/devices/system/cpu/cpuX/topology/book_siblings`
#### 描述：基于相同`book_id`的`cpuX`硬件线程的内核内部映射。仅在`s390`系统中使用。

### 值：十六进制位掩码
#### 何项：`/sys/devices/system/cpu/cpuX/topology/book_siblings_list`
#### 描述：基于相同`book_id`的`cpuX`硬件线程的人类可读列表
#### 格式如：0-3, 8-11, 14,17。仅在`s390`系统中使用。
### 值：         十进制列表
**什么：**      `/sys/devices/system/cpu/cpuX/topology/drawer_siblings`
**描述：**      内核内部映射，表示`cpuX`在同一`drawer_id`下的硬件线程。它仅在s390上使用。
### 值：         十六进制位掩码
**什么：**      `/sys/devices/system/cpu/cpuX/topology/drawer_siblings_list`
**描述：**      易于人类阅读的列表，表示`cpuX`在同一`drawer_id`下的硬件线程。
格式类似于 0-3, 8-11, 14,17。它仅在s390上使用。
### 值：         十进制列表
