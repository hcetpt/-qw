```SPDX 许可证标识符: GPL-2.0

==============================
运行时验证（rv）工具
==============================

**rv** 工具为一系列运行时验证（rv）监视器提供接口。
.. toctree::
   :maxdepth: 1

   rv
   rv-list
   rv-mon
   rv-mon-wip
   rv-mon-wwnr

.. only::  子项目和 html

   索引
   =======

   * :ref:`总索引`
```

请注意，上述翻译中的 `toctree` 和某些标记指令（如 `only` 后面的内容）通常用于 Sphinx 文档系统，并且它们在这里保持未被翻译，因为它们在生成文档结构方面具有特定的功能。同时，`Indices` 下的 `genindex` 是 Sphinx 中指向生成的索引的引用，在中文环境中可以考虑翻译为“总索引”。
